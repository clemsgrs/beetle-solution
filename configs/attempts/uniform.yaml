# Attempt 1 (submitted): base recipe with uniform training-batch ROI sampling.
run:
  output_root: data/beetle/runs/uniform
  tags: [beetle, virchow2, uniform]

training:
  roi_batch_sampling: uniform
  class_request_ratios: null
